// Hand-written game tables. `auth-schema.ts` is the other half of this
// database and is CLI output: `pnpm db:generate-schema` rewrites that file
// whole, so nothing hand-authored survives there. Add tables here instead.
// drizzle.config.ts globs both files when generating migrations.
export {};
